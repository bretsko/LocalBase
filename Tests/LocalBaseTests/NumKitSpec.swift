import Quick
import Nimble
@testable import LocalBase


class NumKitSpec: QuickSpec {
    override func spec() {
        
        describe("Numbers") {
            
            //            it("can be interpreted in diff langs") {
            //                let num = Double.init("пятьсот целых пять десятых",
            //                                 numFormat: .spellOut,
            //                                 .russian)!
            //                expect(num).to(equal(500.5))
            //            }
            
            //TODO: cannot impl
            //            it("can handle diff languages") {
            //
            //                let format = CurrencyFormat.plural
            //                let fmtr = NumFormatter.shared(format)
            //                fmtr.localeID = .ukrainian
            //                //fmtr.minimumFractionDigits = 1
            //                let str1 = NumFormatter.localizedString(from: 5, number: .currencyPlural)
            //                print(str1)
            //                //                expect(str1).to(equal("$5"))
            //
            //                //                let num1 = fmtr.number(from: "$5.4")
            //                //                expect(num1).to(equal(5.4))
            //
            //                // doesn't work !
            //                //fmtr.generatesDecimalNumbers = false
            //                //let num2 = fmtr.number(from: "$5.4")
            //                //expect(num2).to(equal(5))
            //
            //            }
        }
    }
}





